print("Hello, world!")

# In the terminal, if you want to open VS code, 
# simply type code. Accessing a particular directory 
# via the Terminal and then typing "code .", without 
# the "", opens VS Code within the current working 
# directory.

import torch

print(torch.ones(5))